language: cpp

compiler:
 - gcc
 - clang

before_install:
 - sudo apt-get update -qq

install:
 - sudo apt-get install -qq  libeigen3-dev libboost-program-options-dev libboost-regex-dev 

before_script:
 - mkdir build
 - cd build
 - cmake ..

script:
 - make
 
